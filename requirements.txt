fastapi>=0.115
uvicorn>=0.34
pydantic>=2.11
httpx>=0.28
pytest>=8.3
python-dotenv>=1.0

# Optional: only required when running with AGENT_PLANNER=llm
# (LLMPlanner talks to NVIDIA NIM via the OpenAI-compatible SDK)
# openai>=1.40
